# Local development defaults to http://localhost:3000 when this is omitted.
# In Vercel, set this to the public backend origin (without /api or a trailing slash).
VITE_API_URL=https://your-backend-domain.example.com
